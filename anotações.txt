Surgiu por meio de uma guerra;
Passou por vários processos;
Foi inventado e copiado pela Microsoft;
Depois foi para o ecma;
Surgiu o Mozzila;
Depois passou para o Firefox;
2008 o GoogleCrome foi inventado e cresceu;
Veio o V8, e depois foi modificado para o Node;
Assistir vídeos, treinar ler manuais e referências, anotar, resolver problemas;

O "Number.parseInt(variável)" transforma a strinng em number;
O "Number.parsetFloat(variável)" transforma a string em number, com casas decimais;
Usar o "Number(variáver)" se entende em formato de string, e não number;
Para converter number para string usa: "String(variável)", ou "n.toString(variável)";

var s = "JavaScript"
"Eu estou estudando " + s   Forma uma concatenação
`O aluno ${nome} com ${idade} anos, tirou a nota ${nota}` Usa o placehoder / template string

s.lenght diz  quantos caracteres a string tem;
s.toUpperCase()  deixa tudo com letras maiúsculas
s.toLowerCase()  deixa tudo com letras minúsculas

n1.toFixed(2) muda as casas decimais
n1.toFixed(2).replace(".", ",") muda as casas decimais e muda de . para ,
n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) transforma o valor em moeda
